// = assign operator
// == comparison, compare if values are eual
// === strict comparison, compare if values and types are equal

const PI = 3.14;

if (PI !== "3.14") {
  console.log("PI is a string");
}
